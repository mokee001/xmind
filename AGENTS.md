# 相册筛选基准

用户于 2026-09-10 确认：后续相册筛选与主题聚合默认沿用
`c1c5683eb00c232123832a47f25ddd0d5e3e6e56`（`recollections-rules-v1.0.0`）。

涉及选片、生成相册、更换候选图库或迁移成册能力时：

- 先阅读 `docs/recollection-baseline.md` 和 `docs/recollection-rules-v1.0.0.md`。
- 默认成册引擎是 `selection_lab.recollection_feed.build_feed(lab)`，结果入口为
  `/recollections`。`#memories`、`#stories`、`#hybrid`、`#ente`、`#compare`
  保留作实验或历史对照，不得把这些结果当作用户确认的默认成册结果。
- 运行前执行 `python3 tools/verify_recollection_baseline.py --models`；校验失败时
  先报告差异，不得静默切换策略、降低阈值、替换模型或使用旧结果冒充本次生成。
  不具备本地模型的代码检查环境可不加 `--models`，但不能据此宣称推理环境已验证。
- 从完整、当前数据集的合格候选中按主题／旅程聚合，先去重再检查每册最少 12 张、
  最多 24 张；最多 4 个优先推荐不等于只生成 4 个相册。不固定照片 ID、相册数量或封面。
- 保留非照片与低质量过滤；转发／共享照片不因缺少相机 EXIF 就一律排除。
  不伪造拍摄日期、人物或宠物身份，不为凑数混入无主题依据的照片。
- 不改变模型、主题提示词、筛选权重、聚合或去重规则，除非用户明确要求调整。
  获准的调整应建立新版本及独立结果，说明差异并做回归测试，保留此基准以便回退。
- 新图库隔离缓存、历史排除项、结果和偏好；不得覆盖原图、挪用旧图库成员，或上传照片。
- 当前工作区有其他 App、后端和固件修改。不要为恢复此版本整仓 checkout/reset，
  不要覆盖无关修改。此约定不代表 App／线上所有链路已经接入该引擎；迁移须单独实现并验证。

本基准的完整性清单在 `config/recollection_rules_v1.0.0.json`；Git 提交为校验依据，
而不是可随意重写的本地清单。详情与生成、测试命令见 `docs/recollection-baseline.md`。

## Demo 确认与 TestFlight 发布流程（2026-09-10 用户明确要求）

- 后续 App 调整统一先修改 Demo／网页预览，提供可查看的结果，等待用户确认。
- 用户确认本次预览后，即授权将对应调整合入 App 并直接打包、上传 TestFlight；
  不再重复询问是否发布。用户另有暂缓或仅讨论的指示时，遵循最新指示。
- 发布以用户确认的结果为准，保留项目现有功能，合并重复改动，不整体覆盖其他任务的修改。
- 每次保留对应代码快照、安装包、版本及构建号，便于追溯和回退；不把固定照片结果当作规则版本。
- 发布完成需核对 Apple 已处理构建，并检查该构建已分配到用户正在使用的测试组。
  仅“上传成功”或“内部测试中”不足以宣称用户已能收到更新。
- 确认可用后告知具体版本与构建号及手机更新入口；如仍处于 Apple 处理／审核阶段，准确说明状态。
- 具体发布方式与核对记录见 `docs/demo-testflight-workflow.md`。

## 人物识别与展示偏好的位置（2026-09-10 用户进一步明确）

- 保留精选策略作为后台第一层；人物识别用于偏好选择，不为每个人新增精选相簿。
- 第二层展示范围必须是第一层已入选照片的子集。选择人物不会补入原图库照片，
  不会改写第一层候选集，也不会对偏好过滤后的照片再次检查每册最低张数。
- 当前本地原型 `/recollections-v2` 展示最终照片和人物偏好；`/people-v2` 仅用于原始识别核对。
  多选人物匹配任意一人，未归组的人脸不能冒充确定的人物。模型变化后需重新确认失效选择。
- 偏好人物列表只显示第一层精选结果中有照片的人物；不显示零匹配候选。人物头像也来自第一层精选。
  可选列表基于第一层全集，不因第二层当前选择而继续缩小。
- 此原型尚不代表移动 App 或线上相框已经接入；移动端迁移需单独实现和验证。

- 人物偏好默认按第一层精选中唯一照片数降序展示最多 6 人，其余按需展开；不随机抽样或凑数。
  人物合并必须有用户明确确认，记录绑定图库及识别版本，保留原始索引并兼容已保存的别名选择。

## 整墙交互原则（用户确认）

- 用户操作的单位为模板与精选照片组成的完整照片墙。首次预览确认、当前展示、换一组、多留一会儿围绕整墙实现。
- 不新增“不再展示”或要求用户表达不喜欢的入口；主动换组只推进展示，不产生负向偏好。
- 多留一会儿是整墙搭配的正向信号，记录模板、墙版本与照片组合，不推断对其中所有人物或每张照片的喜恶。
- Demo 的本地模拟状态不得冒充设备回执；正式当前展示必须以对应设备成功上屏回执为准。

## Onboarding 人物与主题展示更新（2026-09-11）

- 用户最新要求：本地 onboarding Demo（8772）的可选人物与头像来自完整相册的已有识别分组，不只来自精选结果。
  在此 Demo 中覆盖前文“人物候选仅限精选、零匹配不展示”的约定；原 `/recollections-v2` 与正式 App 尚未迁移。
- 保留明确确认的合并，不把未归组人脸当作确定人物；按全图库唯一照片数降序默认展示 6 个头像，其余展开。
  圆形头像气泡选中放大 1.1 倍，再次点击取消。
- 最终照片墙仍为第一层已入选照片的子集。全相册中有、精选中没有的人物也可选择，但必须提示无匹配，不补图、不静默改为全部。
- 同一设置页加入 App 的六类主题偏好；本地预览只在已确认范围内优先安排匹配主题，不重跑或改写精选规则。
  保留四步引导，去掉连接页中的照片墙缩略图。细则见 `docs/onboarding-demo-v2.2.md`。
- 本次仍为 Demo 修改，等待用户确认后才迁移 App 与 TestFlight。

## 最终四款模板（2026-09-11 用户确认）

- 仅保留 `template_1` 日常拼贴、`template_2` 圣诞手帐、`template_3` 分层抠图拼贴、`denim_pet` 宠物牛仔拼贴。
- 第三款已替换白底画廊；旧 16 款全部退出当前目录，不得作为默认或自动回退恢复。
- 完整配置、预览来源和上线状态见 `docs/current-template-catalog.md`。依赖缺失须明确标为不可用，不能用占位结果冒充真实生成。

## 当前采用的 App Demo（2026-09-16 用户确认）

- 保留 `http://127.0.0.1:8095/#calendar`，权威实现为 `demos/calendar-app/` 的完整回忆日历 Demo B v0.2.0。
- 后续风格、结构及产品逻辑以此版为基础；`demos/home-scenes/` 的后续视觉探索全部保留为历史对照，不默认采用或迁入 App。
- 确认记录、版本标识与源码回退快照见 `docs/homepage-selected-version.md`。不要用 Superdesign 静态草稿覆盖实际 Demo 行为。

- 同日补充确认：保留范围包含同版新用户 landing 和完整四步设置（欢迎→授权→连接→偏好→更新时间→日历）。交付完整新用户预览使用8095/#welcome或“从新用户流程体验”；不能只给8095/#calendar就视为展示了首次流程。

## 首次体验精简（2026-09-16 最新要求）

- 用户要求缩短首次设置，当前8095 Demo v0.3.0改为两页：欢迎直接照片授权 → 连接照片墙；联网成功保存后自动进入日历。
- 蓝牙、本地网络仅连接阶段按需请求；不前置人物/主题/更新时间，不重复确认已选择的设备。此要求覆盖前文“保留四步引导”。
- 新用户沿用自然安排、每天20:00；重新体验必须保留已有偏好、频次、时间及设备名。保存失败可重试保存，不重连。
- 详见 `docs/calendar-onboarding-short-v0.3.md`。当前为Demo，等待用户确认后再迁移App/TestFlight。

## 回忆日历迁移正式 App（2026-09-16 用户确认）

- 用户已明确要求根据现有日历版改 App UI 与框架，并保留正常连接流程；因此 v0.3.0 两步引导已获迁移和既定 TestFlight 发布授权。
- App 首页为回忆日历，无底部 Tab；左上连接状态，右上偏好与设置同级。偏好保持管理后编辑、保存后只读。原整墙操作保留在设置的照片墙管理。
- 真实 BLE 发现、Wi-Fi 配置、设备认领/持久化、家庭与重配网接口不得被 Demo 模拟替代。首次自动结束必须等待真实成功结果与保存完成。
- 日历仅使用当前设备绑定下已确认的展示记录；当前接口无历史图片，使用记录标记和标题/时间，不伪造缩略图。
- 迁移及验证见 `docs/calendar-native-migration-20260916.md`；源码快照和实际 Apple/TestFlight 状态见 `outputs/calendar-native-20260916/release-status.json`。

## 选片后台化（2026-09-22 用户明确）

- 本机快速整理、照片上传整理和后续候选集更新均为自动后台逻辑，用户不可见、无需操作。
- 策略调整不新增前端流程、候选数量／版本展示、整理进度、重播或手动切换入口。保留正常照片授权、设备连接、偏好和更新时间。
- 撤回日历 Demo 中新增的选片体验卡、偏好状态条、设置整理详情及快捷入口；不能用模拟阶段表示真实策略已接入。
- 正式成册引擎与 `/api/generate` 的统一接入仍须在代码中实现和验证；本次撤回界面不代表该迁移已完成。
