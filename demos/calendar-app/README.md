# 回忆日历 App 完整 Demo · B v0.3.4

视觉与页面结构沿用用户选定的回忆日历版。2026-09-16 根据用户要求，将首次体验从欢迎页加四步设置，精简为两页；本次仍为 Demo，待用户查看。

## 入口

```sh
python3 -m http.server 8095 --bind 127.0.0.1 --directory demos/calendar-app
```

- 精简新用户体验：http://127.0.0.1:8095/?v=0.3.0#welcome
- 常规启动：http://127.0.0.1:8095/ （完成过设置则进日历，否则进欢迎页）
- 日历：http://127.0.0.1:8095/#calendar
- 桌面「从新用户流程体验」和设置底部「重新体验新用户流程」会新建引导草稿，保留原已保存偏好。
- `#setup`、`#setup-permissions` 兼容到欢迎页；旧 `#setup-preferences`、`#setup-schedule` 兼容到连接页，照片未授权时返回欢迎页。

## 两页主线

1. **欢迎与照片授权**：欢迎页直接点「授权照片，开始使用」。允许全部或部分照片均可继续；拒绝保留本页并可重试。
2. **连接照片墙**：按需请求蓝牙、本地网络，自动查找设备。点击发现的设备直接进入 Wi-Fi 选择，不再重复确认。联网成功自动保存并进入日历。

不再前置人物、主题或更新时间设置。新用户沿用现有默认：自然安排、每天20:00更新；之后可在偏好和设置中调整。重新体验时保持既有偏好、频次、时间与设备名称，完成只更新连接与照片授权字段。

## 返回与失败

连接页可返回欢迎页，已授予权限保留；离开页面或重新体验会取消未完成的查找、连接和自动保存回调。连接失败可重试。保存失败停在已连接页，可以重试保存而无需重新配网。手动保存会取消待执行的自动提交，避免失败后再次自动提交。

## 选片过程的产品边界（2026-09-22）

用户确认：本机快速整理、候选上传、云端整理和候选集更新均为内部自动处理。页面不展示这些阶段，不增加切换、重播、重新整理或候选数量入口。用户仍通过照片授权、连接、偏好及更新时间完成原有操作。

v0.3.4 移除了 v0.3.1～v0.3.3 新增的日历体验卡、偏好状态条、设置中的整理详情、桌面快捷入口以及模拟计时器，恢复日历原有结构。历史演示代码 `selection-pipeline.js` 保留供快照／实验核对，当前页面不再加载或执行它；旧本地数据不删除。

本次为撤回多余界面。现有 App 已有首次最多读取 300 张、最多 72 张候选的入口，但真实 `/api/generate` 尚未统一接入 `selection_lab.recollection_feed.build_feed(lab)`。不可将旧 Demo 的“正式候选集 v2”描述为已接通或已上线的策略。

实际策略迁移应在原有界面下完成，保留用户授权范围、偏好硬约束与真实上屏回执；迁移验证与发布状态需单独记录。

## 日历、偏好与设置

- 无底部 Tab；日历左上常驻连接状态，右上同级偏好与设置。
- 首次设置后没有过去的展示记录，显示「回忆，会从今天开始」。未来日期不提前揭晓照片。
- 人物、主题、更新时间继续使用已有管理页面；偏好默认只读，管理后可选、保存后恢复只读。
- 查看示例日历是独立的Demo操作，不生成真实上屏记录。

## 模拟边界

原生 HTML/CSS/JS。权限、蓝牙、本地网络、设备发现、Wi-Fi和上屏状态均为本地模拟。不读相册、不收集真实网络凭证、不运行图库推理，不改 App、筛选规则或模板。此版未发布 TestFlight。

主状态：`echooo-calendar-demo-v1`（localStorage）；精简引导草稿：`echooo-calendar-onboarding-draft-v2`（sessionStorage），与旧四步草稿隔离。

## 验证与回退

- 浏览器393×852完整流程与320×740窄屏验证：照片拒绝重试、部分照片授权、蓝牙拒绝重试、按需权限、直接进入Wi-Fi、连接中返回、自动完成进日历、更新时间保持。
- 必要逻辑回归9项通过，覆盖保存失败、手动/自动保存竞态、旧链接、异步取消和已有设置保留。
- 脚本：`outputs/calendar-app/short-onboarding-audit-20260916.cjs`；结果：同名`.txt`。
- 设计决策和范围：`docs/calendar-onboarding-short-v0.3.md`。
- 旧四步完整快照：`outputs/calendar-app/v0.2.0-before-short-onboarding-20260916/source.tar.gz`。
- v0.3完整快照：`outputs/calendar-app/v0.3.0-short-onboarding-20260916/source.tar.gz`。

恢复只处理此Demo目录，不做整仓reset/checkout。浏览器模拟验证不代表原生权限、硬件连接或真机发布验证。
