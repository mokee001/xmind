# D04 · 惊喜与完成反馈 v0.1

独立预览，基于 2026-09-14 用户讨论：手机确认设置与上屏结果，最终画面留在实体照片墙。未迁移 App、未部署、未发布 TestFlight。

启动：在此目录执行 `python3 -m http.server 8784 --bind 127.0.0.1`。

可走完整授权、连接、主题及更新设置，也可使用 App 外的“直接体验设置完成”。正常路径 6 秒准备、3 秒模拟设备刷新；慢速准备为 22 秒。可测试准备失败、设备离线、照片不足，设置保持并提供恢复入口。“模拟离开 App”期间任务继续，返回显示当前状态。

手机所有页面均不渲染最终画面，无结果领取、查看成品、负向偏好入口。页面外折叠区域提供 CSS 实体墙示意，只有当前任务进入 sending 后收到模拟回执才更新。模拟计时不代表真实设备回执或推理耗时。

六类主题仅为界面偏好；不进行照片精选或改动既有精选规则。相册授权、照片范围、配网、生成、回执均为模拟，无私人照片、无上传、无真实设备调用，刷新页面重置状态。自动更新开关表达展示安排，本预览不实现跨天定时器。

验证：`node --test state.test.cjs`；浏览器核对正常完成、离开再返回、失败重试及手机不显示成品。
