# 偏好 Demo 合入项目：2026-09-10

已将本任务确认的页面结构逐项合入 `photo-wall-app` 当前工作区。目标工作区与「整体｜联调测试」共用 `/Users/wanghuan10/Xmind/photo-wall`，当前 Git 分支为 `hardware/waveshare-eink-spectra6`。此前 Demo 是独立演示文件，并非另一个可直接合并的 App 提交，因此使用增量代码整合，没有整仓切换、重置或整份替换 App。

## 已整合

- 沿用现有两页导航，显示为「偏好／我的」；沿用右上角连接状态与设备管理入口。
- 保留另一任务已去掉的上墙预览。精选集、相簿来源、模板不作为偏好选择项。
- 人物、主题复用已有 normal/more/hide 规则，文案为「都可以／多放一些／不要放」。网页人物明确为示例，不推断真实亲属关系。
- 时间范围和时间倾向作为独立详情页，完成后进入草稿，再复用偏好版本保存与回退。校验真实日期和未来日期。
- 新字段扩展旧快照，保留未知字段、既有排除项和更新计划扩展字段。新增中立分类不会自动产生用户改动。
- 首次必需步骤合并为连接与系统授权，授权后自动保存默认偏好；偏好和更新时间仍可在使用后调整。自动完成等待设备偏好读取，避免旧响应覆盖新保存或重复保存。
- 「展示记录」在「我的」主动打开。新增按设备鉴权的 `GET /api/devices/{device_id}/display-history`，只有与当前 revision 匹配的成功上屏回执才入史。覆盖墨水屏、19 寸屏；不拿生成、排队记录冒充展示，不补造旧记录，设备重新绑定后隔离旧账户记录。

## 原有能力保留

多设备添加、切换、重新配网、移除，账户与共同使用者及角色权限，更新计划，偏好版本回退，本地照片识别、宠物抠图和底层生成发布接口均保留。`preferenceStore.js` 原文件未改写。旧手动生成路径仍保留为不可达历史参考，没有恢复模板或预览入口。

## 实际接入边界

- 时间范围／倾向已接入 UI 和版本存储，现有自动选片服务尚未消费这两个字段，页面明确说明，不能视为已经执行的排除边界。
- 现有后台完整自动调度、V1 成册引擎迁入正式 App 生成路径仍是独立工作，本次没有以界面改动冒充已完成。
- 展示记录目前返回标题、确认时间、revision 等文字记录。照片历史回看需要后续受鉴权、不可变的图片存储；本次没有将公开图片 URL 暴露为历史接口。
- 后端代码已整合，现有运行服务未重启，旧服务访问新历史接口时 App 会显示暂不可用。未发布 TestFlight、未推送远端、未操作真实设备。

## 验证与已有差异

- iOS Hermes 与 Web Expo 导出成功；这属于应用打包校验，不等于真机发布验证。
- 3 项 Node 回归覆盖快照扩展不覆盖、旧版本兼容和日期边界；16 项后端 unittest 覆盖展示回执与设备兼容。
- 本次整合前的算法校验发现 `selection_lab/server.py`、`selection_lab/static/index.html`、`tests/test_selection_lab.py` 已不同于冻结基准。差异保留，未重写基准清单或恢复其他任务的代码；本次未生成新相册，未检查模型运行环境。
- 修改前相关文件备份：`outputs/preference-integration-20260910/before/`；本次增量包：`outputs/preference-integration-20260910/integration-only.patch`。
