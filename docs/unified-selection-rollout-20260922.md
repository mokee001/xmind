# 统一选片接入记录 · 2026-09-22

## 当前状态：后端新契约已上线，App 1.3.7（30）已可内部测试

本次用户授权完成真实选片接入并更新安装包。用户将香港服务器升级为 4 vCPU / 8 GiB 后，固定模型的完整推理已在三张公开测试图上运行完成；独立网关已上线。此验证不等于私人图库选片质量或实体设备上屏验收。
App **1.3.7（30）** 已完成原生编译、签名及模块检查；已成功上传 Apple，提交任务 `044c8af4-2987-4015-bf6d-6507f7ef9f38` 为 FINISHED。2026-09-22 16:41:37（上海）核对 Apple 构建 `22896d4f-7c8e-4e30-91dc-2d82d78fae0c` 为 VALID / IN_BETA_TESTING，未过期，已关联原 Team (Expo) 组。签名包的 Hermes 字节码已验证新契约开关为 true。此状态不代表具体手机已经安装。

## 已实现的代码

- App 使用 `EXPO_PUBLIC_UNIFIED_SELECTION=1` 显式启用新契约；未启用时保留既有链路。当前工作区默认仍关闭，发布时须明确开启并核对产物。
- 已授权照片按近期 300 张分页，本机既有强规则初筛；只上传合格候选与真实 Apple Vision 观察数据，不以数量不足为由上传被过滤照片。
- 前台自动执行、后台挂起、网络失败保留进度，下次前台继续。不会承诺 iOS 退后台后无限执行或未打开 App 时读取新照片。
- 文件按内容寻址，账户和来源组合隔离，服务端确认上传后才提交本地检查点。没有新增“快速整理/上传整理”的用户操作。
- 固定基准独立运行，使用 `build_feed(lab)`；正式主题册仍先去重、每册 12–24 张。首次临时候选不能冒充正式成册。
- `/api/generate` 使用已验证快照；人物排除先于主题偏好，人物选择不补入第一层之外照片、不重新检查主题册最低数量。失效的人物选择失败关闭。
- 预览不计作真实上屏，原发布及设备回执链路保持不变。
- 授权及照片来源说明明确合格候选会自动上传，不再沿用“只有主动同步才上传”的不准确文字。

## 为什么采用独立网关

线上 `/opt/photowall/app/backend/server.py` 与本地版本存在大量非本次差异，包含不同的 19 寸设备通信实现。
新增 `backend/selection_gateway.py` 仅接收 `/api/selection/*`，以及带
`X-Selection-Contract: unified-recollections-v1` 的 `/api/generate` 请求。
其他请求保留原 8000 服务。网关运行于 8001，Caddy 已备份、校验并重载。原 server.py 未覆盖，SHA-256 仍为 `255c821821dfbf456e7a57601722fa8aa67850b7b6a1c4bc1acf2cb984cc545d`。
公网 `/api/selection/capabilities` 返回固定契约和 `ready: true`；新契约生成请求使用无效设备凭据时返回 403，原 `/healthz` 正常。

## 验证与阻塞

- 26 项 Python 测试通过，包含账户/来源隔离、错误观测与损坏图片拒绝、正式快照边界、人物稳定 ID、真实模板 PNG 渲染、worker 超时子进程清理与非 root 资源限额；合成数据测试不等于推理质量验收。
- 27 项 Node 测试通过，包含断点队列、回忆日历与真实设备回执。
- 新契约开启时 iOS Metro/Hermes 导出通过，638 模块；这不是原生编译或 IPA 签名验证。
- 原工作区基准校验存在 6 个既有文件差异；没有覆盖它们。隔离固定提交与 5 个模型校验已通过。
- 云端 `/opt/photowall/recollection-v1` 已准备固定 Ente 源码、模型、Rust harness、Dart 生成文件及隔离 Python 依赖。
- 仅上传 3 张公开且哈希固定的测试图片，没有上传用户私人图库。Rust 人脸/内容识别已完成；进入 Flutter 成册运行阶段后，API 与 SSH 均出现超时。
- 此时最后已知服务器内存约 3.4 GiB、无 swap。资源耗尽是待核实假设，不作为已确认根因。远程停止命令未成功送达；不能宣称测试进程已结束或线上已恢复。
- 阿里云控制台已恢复登录。用户授权并完成身份验证后，普通重启于 15:42:35 发起、15:51:30 成功；未使用强制重启。SSH、现有 API、Caddy、aegis 均恢复，API 健康检查返回正常。上一启动周期没有找到可确认原故障的 OOM 记录，原故障根因仍不能直接断言。
- 后续 Linux worker 增加命名 systemd scope 的内存/CPU/进程数和总运行时限制，并在退出时停止整个 scope，避免遗留 Flutter 子进程。安全参数已在服务器通过空任务验证；完整网关尚未部署。
- 15:53:24 开始仅使用原 3 张公开测试图片的限额复验，2 GiB 内存、无交换、单核 CPU、最长 4 分钟。15:55:15 得到明确 `Result=oom-kill`、`MemoryPeak=2147483648`，仍停在 Flutter 测试编译加载阶段；此证据仅证明该限额无法完成，不等于已确认原无响应的根因或全部所需内存。
- 限额失败后 SSH 和原 API 保持正常，无遗留 Flutter/Dart 推理进程；没有创建 `production-ready.json`、没有切换路由或上传新 App。没有购买配置或新增交换空间。
- 早先安装解压依赖触发 needrestart 后 `aegis.service` 启动失败，本次重启后已独立检查为 `active`。

## 接续顺序

1. 重启与现有服务恢复已经确认，复验的内存上限终止证据已保留。需要扩容时先向用户说明，不能自行购买资源。
2. 确认资源方案后再运行：可先评估使用现有空闲磁盘的临时交换空间，或独立推理资源；临时交换也不代表一定成功或性能足够。不得在 API 同机取消资源上限。
3. 在资源限额下完成真实推理；不能换模型/降低规则/用旧结果顶替。
4. 完整验收通过后才创建 `production-ready.json`。独立网关内部验证通过，备份并校验 Caddy 配置后再切换新契约流量。
5. 冻结 App 新版本源码，启用新契约，构建并校验新 IPA。原生照片模块、签名、版本/构建号均须验证。
6. 提交并核对 Apple 处理和原 Team (Expo) 组中的确切构建；随后给用户更新入口，不能仅凭上传成功声称可安装。

本次材料保存在 `outputs/unified-selection-20260922/`；不包含用户相册上传或新的设备上屏回执。

## 16:13 后升级复验与部署（覆盖上述历史阻塞状态）

- 用户自主升级服务器，控制台为 4 vCPU / 8 GiB / 70 GiB；OS 可见内存 7.1 GiB。未自行扩容原 49 GiB 文件系统，仍有空间；没有创建 swap。
- 固定模型与基准不变。5 GiB、2 核限额下，完整真实推理冷运行 124 秒、峰值约 3.6 GiB；重复运行 31 秒。数据仅三张公开测试图，故正式相册为零符合每册至少 12 张要求，未凑图。
- 实际 photowall 非 root 服务账号复验通过，包含原生观察数据、Rust 推理、Flutter/Dart 成册和 `build_feed`，不使用旧输出冒充本次运行。
- 新服务自身限额 1 GiB/1 核；推理通过该账号的 systemd 用户管理器运行，用户 slice 总额 5 GiB/2 核、禁止 swap，并使用跨进程推理锁和超时 scope 清理。模型缓存、输出与用户配置目录具有限定权限。
- 现有照片约 7 GiB 为磁盘数据，不代表常驻内存。没有删除旧图、结果或测试组，没有改选片模型和筛选阈值。
- 回滚：恢复 `/opt/photowall/releases/unified-selection-20260922/Caddyfile.before`，通过 `caddy validate` 后重载，再停止新网关；保留原服务和全部数据。源码位于该发布目录，准备记录位于隔离运行时的 `production-ready.json`。
- App 独立快照提交 `7bdd297c34f261abe047cf867f5b007f40541983`，新契约开关在原生打包环境明确启用。原日历 UI、BLE 配网和设备回执代码保留。完整构建、签名与 Apple 测试组状态仍需后续记录，不能据打包开始声明用户已更新。
